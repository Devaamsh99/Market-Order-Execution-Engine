PORT=3000
NODE_ENV=development

# Runtime Redis (tests do NOT use this)
REDIS_URL=redis://localhost:6379

# Runtime Postgres (tests do NOT use this)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tdbits

# How long to keep active order records/events in Redis (seconds)
ACTIVE_ORDER_TTL_SECONDS=3600

